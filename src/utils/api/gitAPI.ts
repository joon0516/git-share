import { type DirectoryResponse, type FileResponse } from './types';
import { type Result } from '~/utils/result';


//! MAIN FUNCTIONS


export async function getRootLevelFiles(
  repository: string,
  branch: string | null = null,
): Promise<Result<DirectoryResponse, string>> {}

export async function getFilesFromDirectory(
  directory: DirectoryDescriptor,
  repository: string,
  branch: string | null = null,
): Promise<Result<DirectoryResponse, string>> {}

export async function readFile(
  file: FileDescriptor,
  repository: string,
  branch: string | null = null,
): Promise<Result<FileResponse, string>> {}
